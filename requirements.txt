# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt
# Install all dependencies:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# FastAPI — async web framework for building the REST API
fastapi==0.111.0

# Uvicorn — ASGI server that runs the FastAPI application
uvicorn[standard]==0.29.0

# Requests — synchronous HTTP client used by the monitoring engine
# to perform real health checks against each API endpoint
requests==2.32.3

# APScheduler — production-grade background job scheduler
# Replaced the `schedule` library; provides:
#   - thread-safe BackgroundScheduler
#   - misfire_grace_time (handles missed jobs gracefully)
#   - coalesce (avoids thundering-herd on recovery)
#   - max_instances (prevents concurrent check cycles)
APScheduler==3.10.4

# Python-dotenv — loads environment variables from a .env file
python-dotenv==1.0.1

# Aiosmtplib — async SMTP client for future email alerting
aiosmtplib==3.0.1
